/ ブランド入力フォーム
= form_for brand do |f|
  / エラーメッセージ
  = render 'layouts/error_messages', model: f.object
  / ブランドネーム
  .form-group.row
    = f.label :name, 'ブランドネーム ', class:'col-md-3 col-form-label'
    .col-md-9
      = f.text_field :name, class: "form-control"
  / 紹介文
  .form-group.row
    = f.label :content, 'ブランド内容 ', class:'col-md-3 col-form-label'
    .col-md-9
      = f.text_area :content, class: "form-control", rows: "8"
  / ブランド画像
  .form-group.row
    = f.label :picture, 'ブランド画像 ', class:'col-md-3 col-form-label'
    .col-md-9
      = image_tag @brand.picture.url, class:'img-fluid' if @brand.picture?
      = f.file_field :picture, accept: 'image/jpeg,image/gif,image/png'
  / 確認ボタン
  .form-group.row.justify-content-end
    .col-md-9
      = f.submit '投稿する', class:"btn btn-success"
  / 画像チェック
  :javascript
    $('#post_picture').bind('change', function() {
      var size_in_megabytes = this.files[0].size/1024/1024;
      if (size_in_megabytes > 5) {
        alert('最大のファイルサイズは5MBです。ファイルサイズを小さくしてください。');
      }
    });
