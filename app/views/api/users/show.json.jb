# frozen_string_literal: true

{
  user: {
    id: @user.id,
    picture_url: api_asset_path(@user.picture.url),
    name: @user.name,
    gender: @user.gender,
    height: @user.height,
    weight: @user.weight,
    comment: @user.comment
  }
}
