# frozen_string_literal: true

{
  user: {
    id: @user.id,
    picture_url: asset_path(@user.picture_url),
    name: @user.name,
    gender: @user.gender,
    height: @user.height,
    weight: @user.weight,
    comment: @user.comment
  }
}
