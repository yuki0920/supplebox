# frozen_string_literal: true

{
  post: {
    product_id: @post.product_id,
    title: @post.title,
    content: @post.content,
    rate: @post.rate,
    picture_url: @post.picture_url && api_asset_path(@post.picture_url)
  }
}
